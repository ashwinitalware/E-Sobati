import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.sobati.com',
  appName: 'E-Sobati',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
